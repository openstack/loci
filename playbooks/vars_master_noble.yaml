image_tag: "master-ubuntu_noble"
distro_image: "public.ecr.aws/ubuntu/ubuntu:noble"
openstack_release: "master"
# NOTE: Ubuntu Noble provides Ceph Squid packages out of the box
ceph_repo: ""
openstack_images:
  - "{{ barbican_image }}"
  - "{{ cinder_image }}"
  - "{{ cloudkitty_image }}"
  - "{{ cyborg_image }}"
  - "{{ designate_image }}"
  - "{{ glance_image }}"
  - "{{ heat_image }}"
  - "{{ horizon_image }}"
  - "{{ ironic_image }}"
  - "{{ keystone_image }}"
  - "{{ manila_image }}"
  - "{{ neutron_image }}"
  - "{{ nova_image }}"
  - "{{ octavia_image }}"
  - "{{ openstack_client_image }}"
  - "{{ placement_image }}"
  - "{{ tacker_image}}"
  - "{{ watcher_image }}"
